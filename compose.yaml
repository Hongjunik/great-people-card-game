services:
  php:
    image: php:8.4-cli
    ports:
      - "127.0.0.1:8080:8000"
    volumes:
      - type: bind
        source: ./public
        target: /app/public
        read_only: true
    working_dir: /app
    command: ["php", "-S", "0.0.0.0:8000", "-t", "public"]